import { HolderDirective } from './holder.directive';

describe('HolderDirective', () => {
  it('should create an instance', () => {
    const directive = new HolderDirective();
    expect(directive).toBeTruthy();
  });
});
